# คู่มือการ Deploy และจดโดเมนสำหรับ Kossivi Cocktail App

## 📋 สารบัญ
1. [การ Deploy](#การ-deploy)
2. [การจดโดเมน](#การจดโดเมน)
3. [เชื่อมต่อโดเมนกับ Hosting](#เชื่อมต่อโดเมนกับ-hosting)

---

## 🚀 การ Deploy

### ตัวเลือกที่ 1: Vercel (แนะนำ - ง่ายและฟรี)

#### ขั้นตอนการ Deploy บน Vercel:

1. **เตรียมโปรเจกต์**
   ```bash
   # Build โปรเจกต์เพื่อทดสอบ
   npm run build
   ```

2. **ติดตั้ง Vercel CLI (ถ้ายังไม่มี)**
   ```bash
   npm install -g vercel
   ```

3. **Deploy ผ่าน CLI**
   ```bash
   # Login เข้า Vercel
   vercel login
   
   # Deploy โปรเจกต์
   vercel
   
   # Deploy สำหรับ production
   vercel --prod
   ```

4. **หรือ Deploy ผ่านเว็บไซต์**
   - ไปที่ [vercel.com](https://vercel.com)
   - Sign up/Login ด้วย GitHub, GitLab หรือ Bitbucket
   - คลิก "New Project"
   - Import repository จาก GitHub/GitLab
   - Vercel จะ detect โปรเจกต์ Vite อัตโนมัติ
   - คลิก "Deploy"

5. **ตั้งค่า Environment Variables (ถ้ามี)**
   - ไปที่ Project Settings > Environment Variables
   - เพิ่มตัวแปรที่จำเป็น เช่น Supabase URL, API keys

**ข้อดี:**
- ฟรีสำหรับโปรเจกต์ส่วนตัว
- Deploy อัตโนมัติเมื่อ push code
- SSL ฟรี
- CDN อัตโนมัติ
- Custom domain ฟรี

---

### ตัวเลือกที่ 2: Netlify (แนะนำ - ง่ายและฟรี)

#### ขั้นตอนการ Deploy บน Netlify:

1. **สร้างไฟล์ `netlify.toml` ใน root ของโปรเจกต์**
   ```toml
   [build]
     command = "npm run build"
     publish = "dist"
   
   [[redirects]]
     from = "/*"
     to = "/index.html"
     status = 200
   ```

2. **Deploy ผ่านเว็บไซต์**
   - ไปที่ [netlify.com](https://netlify.com)
   - Sign up/Login ด้วย GitHub
   - คลิก "Add new site" > "Import an existing project"
   - เลือก repository
   - ตั้งค่า:
     - Build command: `npm run build`
     - Publish directory: `dist`
   - คลิก "Deploy site"

3. **Deploy ผ่าน CLI**
   ```bash
   # ติดตั้ง Netlify CLI
   npm install -g netlify-cli
   
   # Login
   netlify login
   
   # Deploy
   netlify deploy --prod
   ```

**ข้อดี:**
- ฟรีสำหรับโปรเจกต์ส่วนตัว
- Deploy อัตโนมัติเมื่อ push code
- SSL ฟรี
- Custom domain ฟรี

---

### ตัวเลือกที่ 3: GitHub Pages

#### ขั้นตอนการ Deploy บน GitHub Pages:

1. **ติดตั้ง gh-pages**
   ```bash
   npm install --save-dev gh-pages
   ```

2. **แก้ไข `package.json`**
   ```json
   {
     "scripts": {
       "predeploy": "npm run build",
       "deploy": "gh-pages -d dist"
     },
     "homepage": "https://yourusername.github.io/kossivi-cocktail"
   }
   ```

3. **แก้ไข `vite.config.ts`**
   ```typescript
   import { defineConfig } from 'vite'
   import react from '@vitejs/plugin-react'

   export default defineConfig({
     plugins: [react()],
     base: '/kossivi-cocktail/' // เปลี่ยนเป็นชื่อ repository ของคุณ
   })
   ```

4. **Deploy**
   ```bash
   npm run deploy
   ```

5. **เปิดใช้งาน GitHub Pages**
   - ไปที่ repository บน GitHub
   - Settings > Pages
   - Source: เลือก "gh-pages" branch
   - Save

**ข้อดี:**
- ฟรี
- ใช้งานร่วมกับ GitHub ได้เลย

**ข้อเสีย:**
- ต้องมี base path ใน URL
- ไม่มี custom domain ฟรี (ต้องจ่าย)

---

### ตัวเลือกที่ 4: Cloudflare Pages

#### ขั้นตอนการ Deploy บน Cloudflare Pages:

1. **Deploy ผ่านเว็บไซต์**
   - ไปที่ [dash.cloudflare.com](https://dash.cloudflare.com)
   - เลือก "Workers & Pages" > "Create application" > "Pages"
   - เชื่อมต่อกับ GitHub/GitLab
   - เลือก repository
   - ตั้งค่า:
     - Build command: `npm run build`
     - Build output directory: `dist`
   - คลิก "Save and Deploy"

**ข้อดี:**
- ฟรี
- CDN ที่เร็วมาก
- SSL ฟรี
- Custom domain ฟรี

---

### ตัวเลือกที่ 5: AWS S3 + CloudFront

เหมาะสำหรับโปรเจกต์ที่ต้องการควบคุมมากกว่า

1. **Build โปรเจกต์**
   ```bash
   npm run build
   ```

2. **อัปโหลดไปยัง S3**
   - สร้าง S3 bucket
   - Enable static website hosting
   - อัปโหลดไฟล์ใน `dist/` ไปยัง bucket

3. **ตั้งค่า CloudFront**
   - สร้าง CloudFront distribution
   - ตั้งค่า origin เป็น S3 bucket
   - ตั้งค่า SSL certificate

**ข้อดี:**
- ควบคุมได้มาก
- Scalable สูง

**ข้อเสีย:**
- ต้องจ่ายค่าใช้จ่าย (แต่ถูกมากสำหรับ static site)
- ตั้งค่าซับซ้อนกว่า

---

## 🌐 การจดโดเมน

### ตัวเลือกที่ 1: Namecheap (แนะนำ - ราคาถูก)

1. **ไปที่ [namecheap.com](https://namecheap.com)**
2. **ค้นหาโดเมนที่ต้องการ**
   - พิมพ์ชื่อโดเมนที่ต้องการ
   - เลือก extension (.com, .co.th, .dev, etc.)
3. **เพิ่มลงตะกร้าและชำระเงิน**
4. **ตั้งค่า DNS**
   - ไปที่ Domain List > Manage
   - ตั้งค่า DNS records ตามที่ hosting ต้องการ

**ราคาโดยประมาณ:**
- .com: ~$10-15/ปี
- .co.th: ~$30-40/ปี
- .dev: ~$15-20/ปี

---

### ตัวเลือกที่ 2: Cloudflare Registrar (แนะนำ - ราคาถูกที่สุด)

1. **ไปที่ [cloudflare.com](https://cloudflare.com)**
2. **Sign up/Login**
3. **ไปที่ Registrar**
4. **ค้นหาและซื้อโดเมน**
   - Cloudflare ขายโดเมนในราคา cost price (ไม่ทำกำไร)
   - ราคาถูกที่สุดในตลาด

**ราคาโดยประมาณ:**
- .com: ~$8-10/ปี
- .dev: ~$10-12/ปี

---

### ตัวเลือกที่ 3: Google Domains (ปัจจุบันเป็น Squarespace Domains)

1. **ไปที่ [squarespace.com/domains](https://squarespace.com/domains)**
2. **ค้นหาและซื้อโดเมน**

---

### ตัวเลือกที่ 4: THNIC (สำหรับ .co.th)

สำหรับโดเมน .co.th ต้องจดผ่าน THNIC:

1. **ไปที่ [thnic.co.th](https://thnic.co.th)**
2. **เลือก Registrar ที่เป็นสมาชิก THNIC**
   - เช่น Hosting.co.th, Thaihosting, etc.
3. **กรอกข้อมูลและชำระเงิน**

**ข้อกำหนด:**
- ต้องมีเอกสารยืนยันตัวตน (บัตรประชาชน/หนังสือรับรองบริษัท)
- ต้องมีที่อยู่ในประเทศไทย

---

## 🔗 เชื่อมต่อโดเมนกับ Hosting

### สำหรับ Vercel:

1. **ไปที่ Project Settings > Domains**
2. **เพิ่มโดเมน**
   - พิมพ์ชื่อโดเมน (เช่น example.com)
3. **ตั้งค่า DNS**
   - ไปที่ DNS provider ของคุณ (Namecheap, Cloudflare, etc.)
   - เพิ่ม A record หรือ CNAME ตามที่ Vercel แนะนำ:
     ```
     Type: A
     Name: @
     Value: 76.76.21.21
     
     Type: CNAME
     Name: www
     Value: cname.vercel-dns.com
     ```
4. **รอ DNS propagation (ประมาณ 5-30 นาที)**

---

### สำหรับ Netlify:

1. **ไปที่ Site Settings > Domain management**
2. **คลิก "Add custom domain"**
3. **เพิ่มโดเมน**
4. **ตั้งค่า DNS**
   - เพิ่ม A record หรือ CNAME ตามที่ Netlify แนะนำ:
     ```
     Type: A
     Name: @
     Value: 75.2.60.5
     
     Type: CNAME
     Name: www
     Value: your-site.netlify.app
     ```

---

### สำหรับ Cloudflare Pages:

1. **ไปที่ Pages project > Custom domains**
2. **คลิก "Set up a custom domain"**
3. **เพิ่มโดเมน**
4. **ตั้งค่า DNS**
   - ถ้าโดเมนอยู่ใน Cloudflare จะตั้งค่าอัตโนมัติ
   - ถ้าไม่ใช่ ให้เพิ่ม CNAME record:
     ```
     Type: CNAME
     Name: @ หรือ www
     Value: your-project.pages.dev
     ```

---

## 📝 Checklist ก่อน Deploy

- [ ] ทดสอบ build ในเครื่อง: `npm run build`
- [ ] ตรวจสอบว่า environment variables ถูกต้อง
- [ ] ตรวจสอบ Supabase settings (CORS, allowed origins)
- [ ] ทดสอบ preview: `npm run preview`
- [ ] ตรวจสอบ responsive design
- [ ] ตรวจสอบ SEO meta tags (ถ้ามี)

---

## 🛠️ Tips เพิ่มเติม

### เพิ่ม Environment Variables

สร้างไฟล์ `.env.production` สำหรับ production:
```env
VITE_SUPABASE_URL=your-production-url
VITE_SUPABASE_ANON_KEY=your-production-key
```

### ตั้งค่า CORS ใน Supabase

1. ไปที่ Supabase Dashboard
2. Settings > API
3. เพิ่ม domain ของคุณใน "Allowed Origins"

### เพิ่ม Custom 404 Page

สร้างไฟล์ `404.html` ใน `public/` folder

---

## 📞 สนับสนุน

หากมีปัญหาหรือคำถาม สามารถ:
- ดูเอกสารของแต่ละ platform
- ติดต่อ support ของ hosting provider
- ตรวจสอบ logs ใน dashboard

---

**หมายเหตุ:** คู่มือนี้ครอบคลุมตัวเลือกหลักๆ สำหรับการ deploy static site (React + Vite) และการจดโดเมน สำหรับโปรเจกต์ที่มี backend หรือ database อาจต้องใช้วิธีอื่น เช่น Railway, Render, หรือ AWS
