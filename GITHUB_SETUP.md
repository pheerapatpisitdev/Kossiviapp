# 🔐 คู่มือตั้งค่า GitHub Authentication

## วิธีที่ 1: ติดตั้ง GitHub CLI (แนะนำ)

### ขั้นตอนที่ 1: ติดตั้ง Homebrew (ถ้ายังไม่มี)

เปิด Terminal และรัน:

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

**หมายเหตุ:** ต้องใส่รหัสผ่าน admin ของ Mac

### ขั้นตอนที่ 2: ติดตั้ง GitHub CLI

```bash
brew install gh
```

### ขั้นตอนที่ 3: Login

```bash
gh auth login
```

จะถามคำถาม:
1. **What account do you want to log into?** → เลือก `GitHub.com`
2. **What is your preferred protocol?** → เลือก `HTTPS` (ง่ายกว่า)
3. **Authenticate Git with your GitHub credentials?** → เลือก `Yes`
4. **How would you like to authenticate?** → เลือก `Login with a web browser`
5. จะแสดง code ให้ copy แล้วกด Enter
6. Browser จะเปิดขึ้นมา → paste code และ login

### ขั้นตอนที่ 4: Push Code

```bash
cd /Users/pheerapatpisit/Documents/APP/Kossivi
git push -u origin main
```

---

## วิธีที่ 2: ใช้ Personal Access Token (ง่ายกว่า - ไม่ต้องติดตั้งอะไร)

### ขั้นตอนที่ 1: สร้าง Token บน GitHub

1. ไปที่ GitHub.com
2. คลิกที่รูปโปรไฟล์ (มุมขวาบน) → **Settings**
3. ด้านซ้าย → **Developer settings**
4. ด้านซ้าย → **Personal access tokens** → **Tokens (classic)**
5. คลิก **Generate new token** → **Generate new token (classic)**
6. ตั้งชื่อ token: `Kossiviapp Deploy`
7. เลือก expiration: `90 days` หรือ `No expiration` (ตามต้องการ)
8. เลือก scopes:
   - ✅ **repo** (Full control of private repositories)
9. คลิก **Generate token**
10. **⚠️ สำคัญ:** Copy token ทันที (จะแสดงแค่ครั้งเดียว!)
   - ตัวอย่าง: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### ขั้นตอนที่ 2: Push Code

```bash
cd /Users/pheerapatpisit/Documents/APP/Kossivi
git push -u origin main
```

เมื่อถาม:
- **Username:** `pheerapatpisitdev`
- **Password:** `<paste token ที่ copy มา>` (ไม่ใช่รหัสผ่าน GitHub!)

---

## วิธีที่ 3: ใช้ SSH Key (สำหรับใช้งานระยะยาว)

### ขั้นตอนที่ 1: ตรวจสอบว่ามี SSH Key หรือยัง

```bash
ls -al ~/.ssh
```

### ขั้นตอนที่ 2: สร้าง SSH Key (ถ้ายังไม่มี)

```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

กด Enter เพื่อใช้ default location และตั้ง passphrase (หรือกด Enter ว่างๆ)

### ขั้นตอนที่ 3: Copy Public Key

```bash
cat ~/.ssh/id_ed25519.pub
```

Copy ทั้งหมด (เริ่มจาก `ssh-ed25519` ถึง email)

### ขั้นตอนที่ 4: เพิ่ม Key ไปยัง GitHub

1. ไปที่ GitHub.com
2. Settings → **SSH and GPG keys**
3. คลิก **New SSH key**
4. Title: `Mac - Kossiviapp`
5. Key: paste key ที่ copy มา
6. คลิก **Add SSH key**

### ขั้นตอนที่ 5: เปลี่ยน Remote URL เป็น SSH

```bash
cd /Users/pheerapatpisit/Documents/APP/Kossivi
git remote set-url origin git@github.com:pheerapatpisitdev/Kossiviapp.git
```

### ขั้นตอนที่ 6: Push Code

```bash
git push -u origin main
```

---

## 🎯 แนะนำ: ใช้วิธีไหนดี?

- **วิธีที่ 1 (GitHub CLI):** ดีที่สุดสำหรับใช้งานระยะยาว
- **วิธีที่ 2 (Token):** ง่ายที่สุด ไม่ต้องติดตั้งอะไร
- **วิธีที่ 3 (SSH):** ดีสำหรับผู้ที่ใช้ SSH อยู่แล้ว

---

## ✅ หลังจาก Push สำเร็จ

1. ไปที่ GitHub.com → repository `Kossiviapp`
2. ตรวจสอบว่าไฟล์ทั้งหมดถูก push ขึ้นไปแล้ว
3. กลับไปที่ Vercel และ Deploy ได้เลย!

---

## 🆘 ถ้ามีปัญหา

### Error: "Permission denied"
- ตรวจสอบว่า token หรือ SSH key ถูกต้อง
- ตรวจสอบว่า repository เป็นของคุณจริงๆ

### Error: "Repository not found"
- ตรวจสอบว่า repository `Kossiviapp` ถูกสร้างบน GitHub แล้ว
- ตรวจสอบชื่อ repository และ username ถูกต้อง

### Error: "Authentication failed"
- Token อาจหมดอายุ → สร้าง token ใหม่
- SSH key อาจไม่ถูกเพิ่มใน GitHub → เพิ่มใหม่
