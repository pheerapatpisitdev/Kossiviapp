# 🚀 ขั้นตอน Push Code ไปยัง GitHub (Personal Access Token)

## 📝 ขั้นตอนที่ 1: สร้าง Personal Access Token

### 1.1 ไปที่ GitHub
- เปิด browser ไปที่: https://github.com
- Login เข้าสู่ระบบ

### 1.2 ไปที่ Settings
- คลิกที่ **รูปโปรไฟล์** (มุมขวาบน)
- เลือก **Settings**

### 1.3 ไปที่ Developer Settings
- ด้านซ้าย → เลื่อนลง → คลิก **Developer settings**

### 1.4 ไปที่ Personal Access Tokens
- ด้านซ้าย → คลิก **Personal access tokens**
- คลิก **Tokens (classic)** (ไม่ใช่ Fine-grained tokens)

### 1.5 สร้าง Token ใหม่
- คลิก **Generate new token**
- เลือก **Generate new token (classic)**

### 1.6 ตั้งค่า Token
- **Note:** `Kossiviapp Deploy` (หรือชื่ออะไรก็ได้)
- **Expiration:** เลือก `90 days` หรือ `No expiration` (ตามต้องการ)
- **Select scopes:** 
  - ✅ **repo** (Full control of private repositories)
    - จะเลือก sub-options ทั้งหมดอัตโนมัติ
- คลิก **Generate token** (ด้านล่าง)

### 1.7 Copy Token
- ⚠️ **สำคัญมาก:** Token จะแสดงแค่ครั้งเดียว!
- Copy token ทั้งหมด (เริ่มจาก `ghp_` ถึงท้าย)
- ตัวอย่าง: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
- **เก็บ token ไว้ในที่ปลอดภัย!**

---

## 📝 ขั้นตอนที่ 2: Push Code

### 2.1 เปิด Terminal
- เปิด Terminal หรือ iTerm

### 2.2 ไปที่โฟลเดอร์โปรเจกต์
```bash
cd /Users/pheerapatpisit/Documents/APP/Kossivi
```

### 2.3 Push Code
```bash
git push -u origin main
```

### 2.4 ใส่ Username
เมื่อถาม:
```
Username for 'https://github.com': 
```
พิมพ์: `pheerapatpisitdev`
กด Enter

### 2.5 ใส่ Token (แทน Password)
เมื่อถาม:
```
Password for 'https://pheerapatpisitdev@github.com': 
```
**⚠️ สำคัญ:** วาง token ที่ copy มา (ไม่ใช่รหัสผ่าน GitHub!)
- Token จะไม่แสดงตัวอักษรเมื่อพิมพ์ (ปกติ)
- กด Enter

### 2.6 รอผลลัพธ์
ถ้าสำเร็จจะเห็น:
```
Enumerating objects: XX, done.
Counting objects: 100% (XX/XX), done.
Writing objects: 100% (XX/XX), done.
To https://github.com/pheerapatpisitdev/Kossiviapp.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

---

## ✅ ตรวจสอบผลลัพธ์

1. ไปที่: https://github.com/pheerapatpisitdev/Kossiviapp
2. ตรวจสอบว่าไฟล์ทั้งหมดถูก push ขึ้นไปแล้ว
3. ควรเห็นไฟล์เช่น:
   - README.md
   - package.json
   - src/
   - vercel.json
   - และอื่นๆ

---

## 🎉 เสร็จแล้ว!

ตอนนี้ code อยู่บน GitHub แล้ว สามารถ:
1. กลับไปที่ Vercel
2. Deploy โปรเจกต์ได้เลย!

---

## 🆘 ถ้ามีปัญหา

### Error: "Authentication failed"
- ตรวจสอบว่า token ถูก copy มาครบถ้วน
- ตรวจสอบว่าเลือก scope `repo` แล้ว
- ลองสร้าง token ใหม่

### Error: "Repository not found"
- ตรวจสอบว่า repository `Kossiviapp` ถูกสร้างบน GitHub แล้ว
- ตรวจสอบ username: `pheerapatpisitdev`

### Error: "Permission denied"
- ตรวจสอบว่า token ยังไม่หมดอายุ
- ตรวจสอบว่า repository เป็นของคุณจริงๆ

---

## 💡 Tips

- Token ควรเก็บไว้ในที่ปลอดภัย
- ถ้า token หมดอายุ ต้องสร้างใหม่
- Token ใช้แทน password ได้ทุกครั้งที่ push/pull
